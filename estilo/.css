@charset "UTF-8";
@import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Montez&family=Work+Sans:ital,wght@0,100..900;1,100..900&display=swap');
@font-face{
    font-family: 'Android';
    src: url('../fontes/idroid.otf') format('opentype');
    font-weight: normal;
}
/*
    Para criar um seletor com uma variável global usamos o   *  
*/
*{
    margin: 0px;
    padding: 0px;
}

:root {
    --cor0:#c5ebd6;
    --cor1:#83e1ad;
    --cor2:#3ddc84;
    --cor3:#2fa866;
    --cor4:#1a5c37;
    --cor5:#063d1e;

    --font-padrao:Arial, Helvetica, sans-serif;
    --font-destaque:'Bebas Neue', sans-serif;
    --font-android:'Android', 'cursive';
    

}
body{
    background-color:var(--cor0);
    font-family: var(--font-padrao);
}
header{
    background-image: linear-gradient(to bottom,var(--cor3), var(--cor5));
    min-height: 150px;
    text-align: center;
    padding-top: 40px;
}
header h1 {
    color: white;
    font-family: var(--font-destaque);
    font-size: 3em;
    margin-bottom: 20px;
    text-shadow: 2px 2px 0px rgba(0, 0, 0, 0.397);

}
header p {
    color: white;
    font-family: var(--font-padrao);
    text-shadow: 2px 2px 0px rgba(0, 0, 0, 0.397);
    max-width: 500px;
    margin: auto;
    padding-right: 10px;
    padding-left: 10px;
}
nav{
    background-color: var(--cor5);
    padding: 10px;
    box-shadow: 0px 7px 7px rgba(0, 0, 0, 0.349);
}
nav a{
    text-decoration: none;
    color: white;
    padding: 5px;
    margin: 5px;
    text-shadow: 2px 2px 0px rgba(0, 0, 0, 0.397);
}
nav :hover{
    background-image: linear-gradient(var(--cor2), var(--cor5));
    color: var(--cor1);
    border-radius: 4px;
   
}
main{
    min-width: 300px;
    max-width: 1000px;
    margin: auto;
    margin-bottom: 30px;
    background-color: white;
    padding: 20px;
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.212);
    
}
main h1 {
    font-size: 2em;
    color: var(--cor5);
    font-family: var(--font-android);
}
main h2 {
    color: var(--cor5);
    background-image: linear-gradient(to right, var(--cor1), transparent );
    font-family: var(--font-android);
    text-indent: 10px;
    border-bottom-left-radius:15px;
    border-top-left-radius: 15px;
}
main p{
    margin: 15px 0px;
    text-align: justify;
    text-indent: 30px;
    line-height: 2em;
    
}
main a{
    text-decoration: none;
    font-weight: bold;
    color: var(--cor4);
    background-image: linear-gradient(to right, var(--cor1), transparent);
    padding: 0px 10px;
    border-bottom-left-radius:10px;
    border-top-left-radius: 10px;
}
main a:hover {
    text-decoration: underline;
    color: var(--cor4);
}
main strong{
    color: var(--cor4);
    font-weight: bolder;
    padding: 2px;
}
main img{
    width: 100%;
    
}
main img.pequena{
    max-width: 350PX;
    display: block;
    margin: auto;

}
div.videos{
    background-color: var(--cor4);
    margin-bottom: 30px;
    margin: 0px -20px 30px -20px;
    padding: 20px;
    position: relative;
    padding-bottom: 50%;
}
div.videos  iframe{
    position: absolute;
    top: 5%;
    left: 5%;
    width: 90%;
    height: 90%;


}
aside{
    background-color:var(--cor0);
    padding: 10px;
    border-radius: 10px;
    box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.24);
}
aside h3{
    color: white;
    background-color: var(--cor5);
    margin: -10px -10px 0px -10px;
    text-align: center;
    border-top-right-radius: 10px;
    border-top-left-radius: 10px;
    padding: 5px;
}
aside ul{
    list-style-type: '\2714\00a0\00a0';
    list-style-position: inside;
    columns: 2;  
    line-height: 2em;
}
aside #ro{
    background-color: var(--cor5);
    margin: 0px -10px -10px -10px;
    border-bottom-right-radius: 10px;
    border-bottom-left-radius: 10px;
}
footer{ 
    background-color: var(--cor5);
    margin: auto;
    font-size: 0.8em;
    text-align: center;
    color: white;
   padding: 10px;
}
footer a{
    text-decoration: none;
    color: var(--cor1);
}
footer a:hover{
    text-decoration: underline;
    color: var(--cor3);
}